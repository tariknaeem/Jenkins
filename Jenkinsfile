pipeline {
    agent any
    stages {
        stage("checking ") {
            when {
                branch 'master'
            }
        steps {
            echo "run code"
        }
        }
    }
}