pipeline {
    agent any
    stages {
        stage("checking ") {
            when {
                branch 'main'
            }
        steps {
            echo "run code"
        }
        }
    }
}